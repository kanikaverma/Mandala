# sample program 
# Author: Edo Roth

/# Creates and draws an evenly-spaced Mandala of specified radius with one shape of specified type, and number of shapes to be spaced #/

def Void simpleEvenlySpaced(String shape_name, Number r, Number num_shapes):

  Mandala m = create Mandala

  foreach i = 1 to i = num_shapes:

    Shape temp_shape = create Shape:
      type shape_name
      size r / 5
      rotation 0
      
    Layer temp_layer = create Layer:
      radius r
      Shape temp_shape
      count 1
      offset i * 360 / num_shapes

    addTo: m, temp_layer

draw: m

simpleEvenlySpaced: Circle, 10, 6